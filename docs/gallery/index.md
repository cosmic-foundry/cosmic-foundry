# Gallery

Simulation outputs and visualizations. Cards are added as physics epochs
land results.

```{card} Epoch 0 — Bootstrap
No physics outputs yet. The visual-regression harness is wired up in
`tests/visual/`.
```

# Gallery

Simulation outputs and visualizations. Cards are added as physics modules
land results.

```{card} Bootstrap
No physics outputs yet. The visual-regression harness is wired up in
`tests/visual/`.
```
